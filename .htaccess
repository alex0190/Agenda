 Options +FollowSymLinks

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect non-existent URLs
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . router.php [L]
</IfModule>
